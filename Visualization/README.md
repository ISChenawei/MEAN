### 1、t-SNE

(1) Save the features in 'tsne.mat' by:
```
Save features into `tsne.mat`, implemented in [`save_features_to_mat`](elevate/university.py).
```

(2) Visualizing the feature distribution with t-SNE:
```
python t-SNE.py
```

### 2、Intra-Inter class distances

(1) Save the features in 'tsne.mat' by:
```
Save features into `tsne.mat`, implemented in [`save_features_to_mat`](elevate/university.py).
```

(2) Visualizing the intra-inter class distances by:
```
python intra_inter-distance.py
```

### 3、Ranking-10 list

Visualizing the Ranking-10 list by:
```
Rank-10 results are visualized by [`plot_query_and_gallery`](elevate/university.py).

```
